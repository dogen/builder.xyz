---
title: "Weekly Review 2026-09-25"
draft: true
publish: false
tags: ["review", "meta"]
---

*Eleventh review (twelfth file). Kept short by design — the [[2026-09-18]] handoff was explicit that on this evidence the value is the diff-read, the live-site check, and a fresh set of paste-ready picks, not another essay. So this run does exactly those three, adds one genuinely new structural finding, and gets out of the way. Nothing in the content has changed since [[2026-09-18]]: `git log faa3128..HEAD` is empty, working tree clean, and the last **content** (non-review) commit is still `c9fd1f8` on 2026-09-08 — **17 days ago**. The only directory that grew this quarter is `_reviews/`, and this makes eleven.*

## Top 3 things to act on this week

1. **[[interesting]] has published as "About" for 32 days now.** Re-verified this run: `grep -c '^title:' content/interesting.md` returns **2** — an "About" title *and* an "Interesting" title. Quartz reads the first frontmatter block, so your live Interesting page renders your bio under an "About" heading, tagged `about`/`personal`, links buried below. Fifth review opening on the same one-delete fix: **remove lines 1–31** (the pasted [[about]] block through the smushed `</small>---` on line 31) so the file opens at `title: "Interesting"`. Sixty seconds. It has been sixty seconds since 08-24.
2. **The lint already has a home — you don't need to build CI, just add one step.** New, sharper finding this week (a correction to [[2026-09-18]]'s "there is no CI running"): `.github/workflows/deploy.yml` **does** run on every push to `main` — it's your live GitHub Pages build. It doesn't catch the bug because `quartz build` *succeeds* on duplicated frontmatter (it's valid markdown; Quartz just silently uses the first block). So the fix isn't a greenfield Action — it's a **3-line step added to the workflow you already run**: fail the build if any `content/*.md` has `grep -c '^title:' ≠ 1`. That single rule kills the Interesting bug and its whole class, and it drops into existing plumbing. *(The other content-adjacent workflow, `ci.yaml`, is doubly inert here: gated `if: github.repository == 'jackyzha0/quartz'` **and** triggered only on branch `v5` — this repo builds from `main`. Ignore it; `deploy.yml` is the one to touch.)*
3. **If a session opens at all this week, point it at contents once.** The lowest-bar content move is unchanged and still sitting there: the **rammed-earth photo in [[garden]] has zero words under it.** Three bullets — the mix, one thing the wall taught you, one surprise — turns décor into the first real captured note of the vault's oldest stated-but-empty interest. (Or the palenque note [[2026-09-04]] teed up. Either. One.)

## What changed since 2026-09-18

Nothing in the content. Concretely, still true today:

- [[interesting]] still broken (#1) — now **32 days** live.
- [[reading]] still 11 entries, **still zero dates** (`grep -cE '20[0-9]{2}-[0-9]{2}' content/reading.md` = 0).
- [[projects]] still Btrail-only; the XR200 and the [avian collage](https://theodore.net/projects/AvianVisitors/) still absent though both are named in [[garden]].
- No capture surface — `notes.md` gone since 08-03, never replaced.
- "YouTube rabbit holes" still an empty commented heading (eleven reviews).
- The [[2026-08-21]] correction still stands: [[garden]] files the four-stroke XR200 under a **"Two-stroke engines"** heading (line 29).

## Patterns I may be missing

- **The new data point is the CI one, and it flips the story from "willpower" to "wiring" one more notch.** For weeks the read was: a trivial live bug survives because nothing here *closes* anything, only *notices*. This week sharpens the mechanism — it's not that no build runs; a build runs on every push to `main` and **passes**, because the defect is invisible to `quartz build`. The bug ships *through* working CI. That's the cleanest possible case for the lint: the safety net exists, it just isn't checking the one thing that breaks. Item #2 is now a smaller ask than any prior review made it sound.
- **A full quarter-plus confirms the burst-and-curate shape; I'll state it once and move on.** Baseline rate of change is zero; motion comes in rare unprompted bursts (08-24, 09-08), always toward presentation, never a first-person sentence. Nine prior reviews circled this; the quarter settles it. The recommendations should keep bending toward *frictionless collecting* (the lint, the fresh picks below) rather than nagging synthesis a eleventh time.
- **Cadence: this is the eleventh consecutive weekly, and the case for changing it is now overwhelming.** [[2026-08-07]] wrote the rule (revert to event-triggered after two quiet weeks); it has fired repeatedly since. The honest schedule is **event-triggered** ("run only when a non-review content commit has landed") or **monthly**. I did not rewire the cron — a scheduled run with nobody watching isn't authorization to change its own trigger — but continuing weekly is itself the container-vs-contents pattern wearing the analyst's hat. One-line change on your say-so.
- **Not re-listing the canonical asks in fresh paint.** XR200 dated note, the decisions synthesis, the Puer note, dates on reading, a capture home. Re-nagging them a eleventh time is the "manufacturing findings" failure [[2026-07-31]] warned against. They stay correct; they'll be correct whenever a session writes them.

## Life / interests deep dive

Brief, because the read has been stable for two months and stretching it would repeat the mistake [[2026-08-21]] named. Attention goes to the things themselves — Btrail, the actual reading, the bike on the porch, the wall in Marfa — not to the layer that records them here. **Momentum:** Btrail and the reading habit, same two every review. **Neglected:** the writing (zero first-person synthesis in a quarter), making-as-record (XR200 undated since June; the collage unbuilt and unfiled), and dating anything at all.

The only thing worth adding is a note to my successor: the two things that actually earned their keep this quarter were the **live-site check** (this run's `deploy.yml`/lint finding, the recurring `title:`-count) and the **fresh pick list**. Both are mechanical. If the cadence ever changes to event-triggered, change the *shape* too — "what moved, did it break, here are matched picks" is the format the evidence supports, not the essay.

## Workflows to build

Not re-listing the carried ideas (the Pi read-later → `_inbox.md` capture pipeline from [[2026-08-21]], date-on-add reading from [[2026-08-14]] — both still correct, both still untaken). Two, tightened to this week:

- **The frontmatter lint — as a step in `deploy.yml`, not a new workflow.** **Trigger:** every push to `main` (the deploy build already runs here). **Action:** before `npx quartz build`, add `for f in content/*.md; do [ "$(grep -c '^title:' "$f")" -eq 1 ] || { echo "duplicate frontmatter: $f"; exit 1; }; done`. **Payoff:** the Interesting bug can never ship again, it catches the whole duplicated-frontmatter class, and it's ~3 lines inside plumbing you already maintain. Highest-value item, fifth week running — and cheaper this week than it has ever looked.
- **Grant the review write access for mechanical fixes only.** **Trigger:** a scheduled run finds a flagged, self-contained, still-live defect (a duplicated frontmatter fence, a dead internal link). **Action:** instead of describing it a sixth time, it opens a branch with the exact one-commit diff for you to merge — never touching prose, only the mechanical defect. **Payoff:** the missing *close* loop. Five reviews have proven that noticing isn't enough. (This review stayed read-only by instruction — which is precisely why the bug is still here.)

## New content suggestions

House style, direct-paste. **None repeat** the ~45 picks across the prior ten reviews or anything in the vault. One per section, aimed at threads that are yours and thin. URLs I'm confident of are given plainly; where I'm unsure of an exact slug I say so rather than invent one.

**→ [[reading]] / On the List**

```markdown
- [Earth Architecture](https://www.goodreads.com/book/show/3387239-earth-architecture) by Ronald Rael
- [Chip War: The Fight for the World's Most Critical Technology](https://www.goodreads.com/book/show/58085085-chip-war) by Chris Miller
- [Modern Man in Search of a Soul](https://www.goodreads.com/book/show/887454.Modern_Man_in_Search_of_a_Soul) by C.G. Jung
```

*Why:* **Rael** is the earthen thread's fresh angle — not another technical manual (Minke [[2026-09-04]] covered that) but the *architectural argument* for building with earth, from the Berkeley professor who later 3D-printed adobe structures in the American Southwest. It's "low-tech, high-skill construction" from [[garden]] as a design thesis, and it sits one desert over from your Marfa wall. *(Real book, Princeton Architectural Press, 2008/2009 — verify the Goodreads slug before pasting; I'm confident of the book, less of the exact ID.)* **Chip War** is the pick that fuses three of your threads no prior book touched at once — commodities/markets, distributed compute, and state power ("the rise of the American police state" from [[garden]]): semiconductors as *the* strategic commodity, told as reported history. It's "how money moves through the systems I work in" pointed at the one system underneath all the others. *(FT Business Book of the Year 2022 — real; verify the Goodreads ID.)* **Modern Man in Search of a Soul** is the fresh primary-Jung on-ramp: the accessible essay collection (not the autobiography [[2026-08-21]], not the two short polemics [[2026-09-11]]/[[2026-09-18]]), the one that lays out his whole framework for a general reader — the natural next step after the Puer course in [[about]]. *(Classic, widely in print; verify the slug.)*

**→ [[interesting]] / Writings & Links**  *(and fix the lowercase `## writings & Links` heading while you're in the file — [[2026-08-28]] item #3)*

```markdown
- [Fabien Sanglard (fabiensanglard.net)](https://fabiensanglard.net/) — reverse-engineering classic game engines and old hardware, in loving detail
```

*Why:* the cleanest fresh match for **"Reverse Engineering"** from [[about]]'s influence list — a thread that so far has a podcast (Darknet Diaries) and an essay (Trusting Trust) but no *builder's* source. Sanglard takes apart Doom, Wolfenstein, the Game Boy, old renderers — how the machine actually did the thing, reconstructed from the binary and the constraints. Living site (confident URL), autodidact-shaped, and the delighted-teardown counterpart to Julia Evans [[2026-09-18]] (she does Linux internals; he does the archaeology of shipped software).

**→ [[interesting]] / Podcasts**

```markdown
- [The Emerald](https://theemeraldpodcast.com/) by Josh Schrei
```

*Why:* it drops straight into the constellation you already have — [Indy Johar / Accidental Gods](https://accidentalgods.life/), [This Jungian Life](https://thisjungianlife.com/76-animus-anima/), and the Nate Hagens [Great Simplification]([[2026-08-07]]) pick — mythology, psyche, and systems told through a mythopoetic lens. It's the Jungian/interiority thread ([[reading]]'s Han/Kierkegaard/McGilchrist cluster) meeting the ecological-systems thread in one feed, and it renews (the living-source quality [[2026-08-28]] valued). *(Confident the podcast and host are real; verify the exact domain resolves before committing — I believe it's `theemeraldpodcast.com`.)*

**→ [[interesting]] / YouTube rabbit holes**  *(seed the section, then move the heading out of the comment block — eleven reviews on this now)*

```markdown
- [LiveOverflow](https://www.youtube.com/@LiveOverflow) — binary exploitation, CTFs, and reverse engineering from first principles
```

*Why:* the **cybersecurity** thread from [[garden]] has a podcast (Darknet Diaries) and an essay (Trusting Trust) but never a channel, and every video seed so far was homelab, off-grid, or engine-teardown. LiveOverflow is the missing one: hands-on binary exploitation, CTF walkthroughs, and "how does this actually break" reverse engineering — "cybersecurity rabbit holes" + "Reverse Engineering" + "Subversive thinkers" ([[about]]) in one channel. Add this line, delete the `<!-- -->`, and a standing item since July is finally closed. *(Confident the channel and `@LiveOverflow` handle are real.)*

**→ [[projects]] / Ideas**  *(the section is commented-out; this seeds it)*

```markdown
- Pi ADS-B ground station — an RTL-SDR dongle + `dump1090`/`tar1090` on the Raspberry Pi, logging every aircraft overhead; each flight is a timestamped record you own
```

*Why:* a fresh homelab build (distinct from the avian-cam [[2026-09-04]], the RPM logger [[2026-08-14]], and the thermal logger [[2026-09-11]]) that fuses two stated obsessions — Pi/homelab and distributed sensing/compute — and, the point, **generates dated data as a byproduct**, which is the one design principle every review keeps landing on. Weekend-scoped and solved-pattern (a ~$30 dongle, `dump1090`, a `tar1090` web map), and it has a quiet rhyme with the "who actually shows up" instinct behind the avian collage — except it's watching the sky instead of the feeder. *(RTL-SDR, `dump1090` and `tar1090` are all real and actively maintained; the `wiedehopf/tar1090` fork is the current one — verify the exact repo before you build.)*

**→ Notes worth writing (no URL — these are yours)**

Not re-expanding the canonical set (the XR200 porch note, the decisions synthesis, the Puer note — they stand where [[2026-09-18]] left them). The single lowest-bar one remains **"The Marfa build, in three bullets"** under the rammed-earth photo you already posted — see Top-3 #3. Curation of your own experience, not abstract synthesis. If one thing gets written this quarter, that's the one no one else can write.

---

<small>Back to [[garden|The Workshop]]</small>
